- make an initial run of all the acats
tests (except the ones that are supposed to fail).
if it is easy classify each error as to the failing feature.
by run I mean actually make and run a z80 executable
for each test.  previously the test suite only parsed the tests.
list the test and pass faill in real_failures.txt
fix this ada compiler so the errors
reported in real_failures.txt are fixed.
for this session make a check box sheet listing each error
so it can be resumed if not finished in one session

