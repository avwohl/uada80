-- Ada.Streams body for Z80
-- Minimal implementation since Root_Stream_Type is abstract

package body Ada.Streams is

   -- No body needed - all operations are abstract in Root_Stream_Type
   -- Concrete stream types will provide implementations

end Ada.Streams;
